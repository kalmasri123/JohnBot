import { VoiceState } from "discord.js";

export default async function VoiceStateUpdate(oldState:VoiceState,newState:VoiceState){
    //Check if not speaking and now speaking
    // if(oldState.spe)
}